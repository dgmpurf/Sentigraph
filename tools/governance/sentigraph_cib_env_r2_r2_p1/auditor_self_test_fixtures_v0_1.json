{
  "schema": "sentigraph_cib_env_r2_r2_auditor_self_test_fixtures_v0_2",
  "version": "0.2",
  "case_count": 25,
  "cases": [
    {
      "name": "stdout_positive_multiple_allowlisted_constants",
      "category": "stdout",
      "expected": true,
      "source": "OUTPUT_A = \"A=PASS\"\nOUTPUT_B = \"B=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A, OUTPUT_B)\nprint(OUTPUT_A)\nprint(OUTPUT_B)\n"
    },
    {
      "name": "stdout_negative_fstring",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\nvalue = \"x\"\nprint(f\"A={value}\")\n"
    },
    {
      "name": "stdout_negative_runtime_concatenation",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\nruntime_value = \"x\"\nprint(\"A=\" + runtime_value)\n"
    },
    {
      "name": "stdout_negative_runtime_formatting",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\nruntime_value = \"x\"\nprint(\"A={}\".format(runtime_value))\n"
    },
    {
      "name": "stdout_negative_exception_text",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\ntry:\n    raise RuntimeError()\nexcept RuntimeError as exc:\n    print(str(exc))\n"
    },
    {
      "name": "stdout_negative_path",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\npath = \"C:/private\"\nprint(path)\n"
    },
    {
      "name": "stdout_negative_value",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\nvalue = \"secret\"\nprint(value)\n"
    },
    {
      "name": "stdout_negative_id",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\nrecord_id = \"id-1\"\nprint(record_id)\n"
    },
    {
      "name": "stdout_negative_count",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\ncount = 2\nprint(count)\n"
    },
    {
      "name": "stdout_negative_registry_content",
      "category": "stdout",
      "expected": false,
      "source": "OUTPUT_A = \"A=PASS\"\nAPPROVED_STDOUT = (OUTPUT_A,)\nregistry_content = \"x\"\nprint(registry_content)\n"
    },
    {
      "name": "reparse_positive_direct_attribute",
      "category": "reparse",
      "expected": true,
      "source": "import stat\nfor entry in entries:\n    file_stat = entry.stat(follow_symlinks=False)\n    if file_stat.st_file_attributes & stat.FILE_ATTRIBUTE_REPARSE_POINT:\n        continue\n    visit(entry)\n"
    },
    {
      "name": "reparse_positive_safe_getattr",
      "category": "reparse",
      "expected": true,
      "source": "import stat\nfor entry in entries:\n    file_stat = entry.stat(follow_symlinks=False)\n    if getattr(file_stat, \"st_file_attributes\", 0) & stat.FILE_ATTRIBUTE_REPARSE_POINT:\n        continue\n    visit(entry)\n"
    },
    {
      "name": "reparse_negative_missing_bitmask",
      "category": "reparse",
      "expected": false,
      "source": "for entry in entries:\n    file_stat = entry.stat(follow_symlinks=False)\n    if getattr(file_stat, \"st_file_attributes\", 0):\n        continue\n    visit(entry)\n"
    },
    {
      "name": "reparse_negative_missing_skip",
      "category": "reparse",
      "expected": false,
      "source": "import stat\nfor entry in entries:\n    file_stat = entry.stat(follow_symlinks=False)\n    if getattr(file_stat, \"st_file_attributes\", 0) & stat.FILE_ATTRIBUTE_REPARSE_POINT:\n        mark(entry)\n    visit(entry)\n"
    },
    {
      "name": "reparse_negative_detection_followed_by_descent",
      "category": "reparse",
      "expected": false,
      "source": "import stat\nfor entry in entries:\n    file_stat = entry.stat(follow_symlinks=False)\n    if getattr(file_stat, \"st_file_attributes\", 0) & stat.FILE_ATTRIBUTE_REPARSE_POINT:\n        walk(entry)\n    visit(entry)\n"
    },
    {
      "name": "reparse_negative_normalization_fallback",
      "category": "reparse",
      "expected": false,
      "source": "import os\nimport stat\nfor entry in entries:\n    normalized = os.path.normcase(entry.path)\n    file_stat = entry.stat(follow_symlinks=False)\n    if getattr(file_stat, \"st_file_attributes\", 0) & stat.FILE_ATTRIBUTE_REPARSE_POINT:\n        continue\n    visit(normalized)\n"
    },
    {
      "name": "broadcast_positive_exact_bounded_after_readbacks",
      "category": "broadcast",
      "expected": true,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_missing_broadcast",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_wrong_message",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001B\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_wrong_lparam",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"NotEnvironment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_before_readback",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    _broadcast_environment_change()\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_dynamic_lparam",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        runtime_lparam = ENVIRONMENT_BROADCAST_LPARAM\n        environment_text = ctypes.c_wchar_p(runtime_lparam)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_non_timeout_send",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_ignored_return",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    },
    {
      "name": "broadcast_negative_retry_or_second_broadcast",
      "category": "broadcast",
      "expected": false,
      "source": "import ctypes\nimport ctypes.wintypes\nimport winreg\n\nHWND_BROADCAST = 0xFFFF\nWM_SETTINGCHANGE = 0x001A\nSMTO_ABORTIFHUNG = 0x0002\nENVIRONMENT_BROADCAST_LPARAM = \"Environment\"\nBROADCAST_TIMEOUT_MS = 2000\n\nOUTPUT_BROADCAST_PASS = \"WINDOWS_ENVIRONMENT_CHANGE_BROADCAST=PASS\"\nOUTPUT_RESULTS_DIR_PASS = \"RESULTS=PASS\"\nOUTPUT_EXPORT_ROOT_PASS = \"EXPORT=PASS\"\nOUTPUT_ADAPTER_ID_PASS = \"ADAPTER=PASS\"\nOUTPUT_REPAIR_PASS = \"REPAIR=PASS\"\nOUTPUT_BROADCAST_BLOCKED = \"BROADCAST=BLOCKED\"\n\n\nclass BroadcastBlocked(Exception):\n    pass\n\n\ndef _broadcast_environment_change():\n    try:\n        user32 = ctypes.WinDLL(\"user32\", use_last_error=True)\n        send_message_timeout_w = user32.SendMessageTimeoutW\n        send_message_timeout_w.argtypes = (\n            ctypes.wintypes.HWND,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.WPARAM,\n            ctypes.wintypes.LPARAM,\n            ctypes.wintypes.UINT,\n            ctypes.wintypes.UINT,\n            ctypes.POINTER(ctypes.c_size_t),\n        )\n        send_message_timeout_w.restype = ctypes.wintypes.LPARAM\n        environment_text = ctypes.c_wchar_p(ENVIRONMENT_BROADCAST_LPARAM)\n        lparam_value = ctypes.cast(environment_text, ctypes.c_void_p).value\n        if lparam_value is None:\n            raise BroadcastBlocked()\n        message_result = ctypes.c_size_t(0)\n        ctypes.set_last_error(0)\n        broadcast_result = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        broadcast_result_second = send_message_timeout_w(\n            HWND_BROADCAST,\n            WM_SETTINGCHANGE,\n            0,\n            lparam_value,\n            SMTO_ABORTIFHUNG,\n            BROADCAST_TIMEOUT_MS,\n            ctypes.byref(message_result),\n        )\n        if broadcast_result == 0:\n            raise BroadcastBlocked()\n    except BroadcastBlocked:\n        raise\n    except Exception as exc:\n        raise BroadcastBlocked() from exc\n\n\ndef _write_and_readback_current_user_environment(values):\n    try:\n        with winreg.OpenKey(winreg.HKEY_CURRENT_USER, \"Environment\") as key_handle:\n            for name, value in values:\n                winreg.SetValueEx(key_handle, name, 0, winreg.REG_SZ, value)\n            for name, expected in values:\n                actual, actual_type = winreg.QueryValueEx(key_handle, name)\n                if actual_type != winreg.REG_SZ or actual != expected:\n                    raise RuntimeError()\n    except OSError as exc:\n        raise RuntimeError() from exc\n    _broadcast_environment_change()\n\n\ndef _run():\n    _write_and_readback_current_user_environment(())\n    print(OUTPUT_BROADCAST_PASS)\n    print(OUTPUT_RESULTS_DIR_PASS)\n    print(OUTPUT_EXPORT_ROOT_PASS)\n    print(OUTPUT_ADAPTER_ID_PASS)\n    print(OUTPUT_REPAIR_PASS)\n\n\ndef main():\n    try:\n        _run()\n    except BroadcastBlocked:\n        print(OUTPUT_BROADCAST_BLOCKED)\n        return 1\n    except Exception:\n        return 1\n    return 0\n"
    }
  ]
}
