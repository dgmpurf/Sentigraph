"""Safe public-page parser scaffolds.

The public parser package is fixture-first and disabled for live fetching by
default. It must not implement login bypass, captcha bypass, proxy rotation,
cookie-based access, or private data collection.
"""

