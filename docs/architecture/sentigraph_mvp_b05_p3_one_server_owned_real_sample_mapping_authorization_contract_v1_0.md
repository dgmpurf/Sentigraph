# Sentigraph MVP-B05-P3 One Server-owned Real Sample Mapping Authorization Contract v1.0

## 1. Decision, privacy, and docs-only scope

```text
Decision = ready
privacy_issue_stop = no
docs_only = yes
read_only_committed_repository_audit = yes
mapping_implementation_performed = no
runtime_action_performed = no
```

This contract freezes governance for exactly one future immutable,
server-owned B05 real-sample mapping candidate. It does not add the mapping,
change the default registry, enable a gate, access protected material, import
the application, call a route, or authorize a smoke.

Only committed documentation, the exact allowlisted source and test files, and
bounded Git metadata were read. No product, test, route, API, frontend,
configuration, Project Source, tag, or release change is part of B05-P3.

## 2. Exact approval and Goal lifecycle

```text
approval SHA-256 = 1d1e6d30eb5b9d49646649959dde597242ad231338c109b7e268054833a9b64d
Goal requested = MVP-B05-P3 One Server-owned Real Sample Mapping Authorization Contract
Goal activation verified = yes
Goal completion = pending_ready_only_terminal_receipt
MVP_B05_P3_approval_reusable = no
MVP_B05_P3_Goal_reusable = no
MVP_B05_P3_status = candidate_completed_pending_independent_ChatGPT_acceptance
```

The exact approval record is:

```text
APPROVE_SENTIGRAPH_MVP_B05_P3_ONE_SERVER_OWNED_REAL_SAMPLE_MAPPING_AUTHORIZATION_CONTRACT_DOCS_ONLY_READ_ONLY_REPOSITORY_AUDIT_STARTING_COMMIT_C9DBE36AB4EC4CC131858D2C07DC017AE3047CFF_PRESERVE_BASELINE_V1_7_EFFECTIVE_WITH_BUDGET_FIXED_CONDITIONAL_RISK_2_4_2_AND_CONSUMED_ENGINEERING_FIXED_CONDITIONAL_RISK_0_0_0_0_AND_REMAINING_FIXED_CONDITIONAL_RISK_2_4_2_AUTHORIZE_ONE_FRESH_SENTIGRAPH_GOAL_AND_CONSUME_EXACTLY_ONE_BASELINE_V1_7_FIXED_PROMPT_RESULTING_IN_CONSUMED_ENGINEERING_FIXED_CONDITIONAL_RISK_1_1_0_0_AND_REMAINING_FIXED_CONDITIONAL_RISK_1_4_2_PRESERVE_MVP_B01_B02_B03_B04_B05_P1_AND_EFFECTIVE_B05_P2_COMPLETED_AND_INDEPENDENTLY_ACCEPTED_PRESERVE_B05_P2_INITIAL_NEEDS_FIX_AND_RC1_FORWARD_REPAIR_AS_DISTINCT_HISTORY_PRESERVE_B03_B04_52_FIELD_IN_MEMORY_REVIEW_ONLY_CANDIDATE_PROJECTION_AND_F10_46_FIELD_PERSISTED_GOVERNED_RECORD_PROJECTION_AS_SEPARATE_NONINTERCHANGEABLE_SURFACES_PRESERVE_B05_ROUTE_SERVICE_GATE_AND_FRONTEND_IMPLEMENTED_SYNTHETIC_FIRST_PRESERVE_DEFAULT_REGISTRY_EMPTY_REAL_SAMPLE_MAPPING_ABSENT_UNAUTHORIZED_AND_DISABLED_AUTHORIZE_EXACT_ONE_DOCS_ONLY_ARCHITECTURE_AND_AUTHORIZATION_CONTRACT_TO_FREEZE_ONE_FUTURE_SERVER_OWNED_IMMUTABLE_MAPPING_ENTRY_WITH_SAMPLE_HANDLE_HELLDIVERS2_PSN_DEMO_AND_RESULT_BASENAME_PROVIDER_RESULT_HELLDIVERS2_PSN_DEMO_20260614_055754_JSON_BIND_THE_MAPPING_CANDIDATE_TO_ACCEPTED_B04_ARTIFACT_SHA256_6297F09939B205877940D1DE964F9D7A0A6DEC1F5817D7A6520949357CF8E553_AS_GOVERNANCE_EVIDENCE_ONLY_WITHOUT_REOPENING_OR_REHASHING_THE_ARTIFACT_FREEZE_REGISTRY_OWNER_BACKEND_APP_SERVICES_INTERNAL_ALPHA_LOCAL_EXCHANGE_REVIEW_PROJECTION_PY_REGISTRY_SCHEMA_SENTIGRAPH_INTERNAL_ALPHA_LOCAL_EXCHANGE_SAMPLE_REGISTRY_V0_1_ROUTE_MODE_INTERNAL_ALPHA_READ_ONLY_LOCAL_EXCHANGE_PROJECTION_OPERATOR_CAPABILITY_B05_LOCAL_EXCHANGE_PROJECTION_READ_ONLY_REQUIRE_THE_FUTURE_MAPPING_ENTRY_TO_USE_THE_EXISTING_FROZEN_DATACLASS_FIELDS_SAMPLE_HANDLE_RESULT_FILE_NAME_ENABLED_ROUTE_MODE_AND_CAPABILITY_LABEL_ONLY_WITH_NO_PATH_ROOT_ADAPTER_HASH_PACKAGE_PROVIDER_COLLECTOR_OR_CONFIGURATION_FIELD_ADDED_TO_THE_REGISTRY_ENTRY_FREEZE_EXACT_SERVER_OWNED_B01_CONFIGURATION_VARIABLE_NAMES_WITHOUT_READING_OR_RECORDING_VALUES_SENTIGRAPH_LOCAL_EXCHANGE_RESULTS_DIR_SENTIGRAPH_PRIVATE_COLLECTOR_EXPORT_ROOT_SENTIGRAPH_LOCAL_EXCHANGE_ADAPTER_ID_FREEZE_NO_SUBSTITUTION_NO_ALTERNATE_HANDLE_NO_ALTERNATE_BASENAME_NO_DIRECTORY_DISCOVERY_NO_GLOB_NO_LATEST_NO_FALLBACK_NO_CLIENT_FILENAME_PATH_ROOT_ADAPTER_PROVIDER_PACKAGE_COLLECTOR_OR_CONFIGURATION_INPUT_REQUIRE_FUTURE_MAPPING_IMPLEMENTATION_TO_BE_A_SEPARATELY_APPROVED_FIXED_MILESTONE_WITH_PRODUCT_CODE_AND_DIRECTLY_COUPLED_TEST_SCOPE_NO_ARTIFACT_PACKAGE_ENDPOINT_APPLICATION_IMPORT_NETWORK_DATABASE_PERSISTENCE_OR_BROWSER_ACCESS_REQUIRE_ANY_FUTURE_REAL_MAPPING_ROUTE_SMOKE_TO_BE_A_SEPARATELY_APPROVED_RISK_MILESTONE_AFTER_IMPLEMENTATION_AND_INDEPENDENT_ACCEPTANCE_WITH_ONE_FRESH_ARTIFACT_IDENTITY_VERIFICATION_ONE_B05_GET_ZERO_RETRY_ZERO_SECOND_GET_AND_EXACT_B01_B03_CALL_COUNTS_ONE_ONE_REQUIRE_FAIL_CLOSED_HANDLING_FOR_ARTIFACT_IDENTITY_MISMATCH_MISSING_FILE_CONFIGURATION_MISMATCH_REGISTRY_MISMATCH_DISABLED_GATE_UNKNOWN_HANDLE_SCHEMA_DEVIATION_OR_AMBIGUOUS_PROTECTED_STATE_AUTHORIZE_EXACT_ONE_FILE_CHANGED_ALLOWLIST_DOCS_ARCHITECTURE_SENTIGRAPH_MVP_B05_P3_ONE_SERVER_OWNED_REAL_SAMPLE_MAPPING_AUTHORIZATION_CONTRACT_V1_0_MD_NO_ARTIFACT_PACKAGE_RUNTIME_APPLICATION_IMPORT_ENDPOINT_PROVIDER_COLLECTOR_NETWORK_LLM_BROWSER_DATABASE_PERSISTENCE_CODE_TEST_CONFIG_ROUTE_API_FRONTEND_PROJECT_SOURCE_TAG_OR_RELEASE_CHANGE_READY_ONLY_COMMIT_MESSAGE_DEFINE_MVP_B05_P3_REAL_SAMPLE_MAPPING_AUTHORIZATION_CONTRACT_AND_PUSH_DO_NOT_AUTHORIZE_MAPPING_IMPLEMENTATION_REAL_MAPPING_ACTIVATION_ARTIFACT_OR_PACKAGE_ACCESS_ENDPOINT_REPLAY_OR_SMOKE_B05_P4_OR_ANY_RUNTIME_ACTION
```

The approval and Goal became consumed and nonreusable when Goal activation was
verified. A blocked or needs-fix outcome after activation would remain
consumed. All audit, authoring, validation, commit, and push actions in this
Goal are sub-actions of the same fixed Prompt. No future commit SHA is asserted.

## 3. Baseline v1.7 accounting

```text
baseline = sentigraph_internal_alpha_mvp_master_completion_baseline_v1_7
baseline effective = yes
Prompt classification = fixed
budget fixed/conditional/risk = 2/4/2

accounting before activation engineering/fixed/conditional/risk = 0/0/0/0
remaining before activation fixed/conditional/risk = 2/4/2

accounting after activation engineering/fixed/conditional/risk = 1/1/0/0
remaining after activation fixed/conditional/risk = 1/4/2

accounting final engineering/fixed/conditional/risk = 1/1/0/0
remaining final fixed/conditional/risk = 1/4/2
```

B05-P3 consumes exactly one fixed Prompt. Its docs audit, contract authoring,
static validation, and ready-only Git finalization are not separately charged.
Budget availability does not authorize B05-P4, B05-P5, or runtime work.

## 4. Accepted checkpoint history

```text
MVP-B01 = completed_and_independently_accepted
MVP-B02 = completed_and_independently_accepted
MVP-B03 = completed_and_independently_accepted
MVP-B04 = completed_and_independently_accepted
MVP-B05-P1 = completed_and_independently_accepted
effective MVP-B05-P2 = completed_and_independently_accepted_via_RC1

latest repository checkpoint before B05-P3 = c9dbe36ab4ec4cc131858d2c07dc017ae3047cff
latest runtime evidence checkpoint = 61581c1d63350a85654493c3aaa1c7b01c4fc950
B05-P2 implementation commit = 65bd00ba38881899f776f90e3d704648e7cb43db
B05-P2-RC1 commit = cae4c0ecf79291d8e9a42ad5b88b7331c509fbf0
```

The B05-P2 initial independent result remains
`needs_fix_directly_coupled_legacy_8Z22_regression`. RC1 forward-repaired the
legacy assertion, and the initial needs-fix is not reclassified, erased, or
merged with the accepted RC1 result.

## 5. B03/B04 and F10 semantic-separation matrix

| Contract property | B03/B04 | F10 |
| --- | --- | --- |
| Exact field count | `52` | `46` |
| Object meaning | In-memory review-only candidate projection | Persisted governed nonproduction record projection |
| Persistence proof | None | Existing persisted-record and reservation lineage only |
| B05 use | Direct 52-field response semantics | Forbidden as a B05 conversion or fallback |
| Interchangeable | No | No |

B03/B04 must not be represented as F10. F10 must not be used to prove B03/B04
persistence. The mapping candidate changes only server-owned selection of one
B03/B04 input basename; it grants no F10 identity, persistence, trust, or
mutation authority.

## 6. Current default-empty B05 state

The accepted committed state at B05-P3 authoring is:

```text
B05 route/service/gate/frontend = implemented_synthetic_first
B05 service blob = f4b3d87c370ca91bb85504552f1c325ea5f15b58
current DEFAULT_SAMPLE_REGISTRY entries = 0
real mapping currently implemented = no
real mapping currently present = no
real mapping currently authorized for runtime = no
real mapping currently enabled = no
```

The current service constructs `DEFAULT_SAMPLE_REGISTRY` with no entries. The
approved handle and real basename do not occur in that service. B05-P3 does not
change either fact.

## 7. Exact future mapping candidate

Exactly one future candidate is defined:

| Candidate property | Exact future value |
| --- | --- |
| `sample_handle` | `helldivers2-psn-demo` |
| `result_file_name` | `provider_result_helldivers2-psn-demo_20260614_055754.json` |
| `enabled` | `true` |
| `route_mode` | `internal_alpha_read_only_local_exchange_projection_operator` |
| `capability_label` | `b05_local_exchange_projection_read_only` |

The temporal boundary is exact:

```text
current mapping present = no
current default registry entries = 0
current mapping enabled = no
future candidate mapping defined by contract = yes
future candidate mapping implemented = no
future candidate mapping activated in product code = no
future route smoke executed = no
```

The proposed future `enabled = true` value belongs only to a later separately
approved implementation. It does not alter the current registry or override
any independently disabled gate.

## 8. Exact five-field registry-entry contract

```text
registry owner = backend/app/services/internal_alpha_local_exchange_review_projection.py
registry schema = sentigraph_internal_alpha_local_exchange_sample_registry_v0_1
registry container = process-local MappingProxyType
entry type = InternalAlphaLocalExchangeSampleRegistryEntry
entry field count = 5
entry fields in exact semantic order = sample_handle, result_file_name, enabled, route_mode, capability_label
extra entry fields proposed = none
```

The future entry must remain an instance of the existing frozen, slotted
dataclass. It must not add a path, directory, root, adapter, artifact hash,
package, provider, collector, configuration, endpoint, trust status, review
status, or runtime-state field. The artifact SHA and server configuration
remain separate governance and server-owned boundaries.

## 9. Artifact governance binding

```text
accepted B04 artifact basename = provider_result_helldivers2-psn-demo_20260614_055754.json
accepted B04 artifact SHA-256 = 6297f09939b205877940d1de964f9d7a0a6dec1f5817d7a6520949357cf8e553
binding role = governance_evidence_for_exact_future_mapping_candidate
artifact accessed/reopened/rehashed/parsed in B05-P3 = 0/0/0/0
absolute artifact path recorded = no
```

The accepted B04 report is the evidence source. B05-P3 did not reopen, hash,
parse, copy, move, enumerate, search for, or otherwise access the artifact.

The SHA-256 is an expected value for a future separately approved protected
smoke. It is not a registry field, frontend field, API response field,
environment variable, runtime configuration value, or authority to access the
artifact.

## 10. Server-owned configuration-name and identity policy

Only these existing variable names are frozen:

```text
SENTIGRAPH_LOCAL_EXCHANGE_RESULTS_DIR
SENTIGRAPH_PRIVATE_COLLECTOR_EXPORT_ROOT
SENTIGRAPH_LOCAL_EXCHANGE_ADAPTER_ID
```

```text
configuration variable names frozen = yes
configuration values accessed = no
configuration values recorded = no
configuration values hashed or inferred = no
configuration value identity proven by B05-P3 = no
committed safe configuration identity evidence = partial_collector_root_only_not_complete_three_variable_binding
configuration_value_identity_binding = unavailable_from_current_committed_safe_evidence
```

The accepted B04 report contains partial non-sensitive root-identity evidence,
but it does not bind the exact value identity of all three variables as one
accepted B05 configuration. It therefore cannot be reused as a complete B05
configuration binding, and no replacement hash is fabricated.

A later separately governed, safe configuration-identity binding is required
before protected artifact access or a B05 GET. Its absence does not block this
docs-only contract or a future no-runtime P4 mapping implementation. A future
implementation may reference the existing names but may not set, migrate,
duplicate, serialize, log, or expose their values.

## 11. Exact gate conjunction

All five gates remain required and disabled by default:

```text
SENTIGRAPH_INTERNAL_ALPHA_REVIEW_CONSOLE_ROUTE_ENABLED
SENTIGRAPH_INTERNAL_ALPHA_LOCAL_EXCHANGE_REVIEW_PROJECTION_ENABLED
SENTIGRAPH_INTERNAL_OPERATOR_STAGING_ROUTE_ENABLED
SENTIGRAPH_INTERNAL_OPERATOR_STAGING_LOCAL_EXCHANGE_ENABLED
SENTIGRAPH_INTERNAL_OPERATOR_STAGING_LOCAL_EXCHANGE_REVIEW_PROJECTION_ENABLED
```

A registry entry alone cannot activate a route, enable a gate, provide server
configuration, access an artifact, import the application, cause a request,
weaken B01/B03, invoke F10, or create persistence or mutation authority.

## 12. No-substitution and client-input prohibition

```text
exact allowed handle count = 1
exact future mapping count = 1
alternate handles = forbidden
alternate basenames = forbidden
duplicate handles = forbidden
fallback entries = forbidden
latest-file selection = forbidden
directory discovery = forbidden
glob or walk = forbidden
search = forbidden
automatic replacement = forbidden
client override = forbidden
```

The result filename, result directory, export root, collector root, package
path, adapter ID, artifact hash, provider schema, latest selector, fallback
selector, and configuration override must never become client input through a
path, query, body, header, browser storage, frontend state, or configuration
control. An unknown or malformed handle remains fail-closed and may not select
the real mapping by inference.

## 13. Future B05-P4 implementation candidate and bounded allowlist

```text
MVP-B05-P4 = One Immutable Server-owned Real Sample Mapping Implementation
MVP_B05_P4_candidate_defined = yes
MVP_B05_P4_selected = no
MVP_B05_P4_authorized = no
MVP_B05_P4_Goal_authorized = no
MVP_B05_P4_executed = no
```

The bounded audit proves that a simple future P4 candidate needs exactly these
three files:

1. `backend/app/services/internal_alpha_local_exchange_review_projection.py`
2. `backend/app/tests/test_mvp_b05_p2_internal_alpha_local_exchange_review_projection.py`
3. `docs/health/sentigraph_mvp_b05_p4_one_immutable_server_owned_real_sample_mapping_implementation_report_v1_0.md`

The service would contain only the minimum immutable default-registry
construction for the approved five values. The focused test is mechanically
coupled because it currently asserts an empty default registry, asserts that
the real handle is absent from the service, and uses the default registry for
unknown-handle and no-file-access cases. A future P4 test repair must preserve
those safety cases by injecting an explicit empty registry where appropriate;
it must not access the real artifact.

The audited 8Z22 and 8Z30 tests constrain route inventory, F10/B05 separation,
frontend behavior, and mutation absence but do not assert the default registry
count or mapping absence. They are therefore excluded from the P4 candidate
allowlist. No route, API-registration, frontend, configuration, manifest, or
lockfile change is justified by the committed evidence.

Future P4 candidate limits remain:

```text
artifact/package access = 0
endpoint calls = 0
application imports = 0
browser activity = 0
external network = 0
database/persistence = 0
real B01/B03 runtime calls = 0
```

No B05-P4 implementation is selected or authorized by this contract.

## 14. Future B05-P5 protected-smoke candidate and counters

```text
MVP-B05-P5 = One Real Sample-handle Governed Read-only Projection Smoke
MVP_B05_P5_candidate_defined = yes
MVP_B05_P5_selected = no
MVP_B05_P5_authorized = no
MVP_B05_P5_Goal_authorized = no
MVP_B05_P5_executed = no
```

P5 may become eligible only after independent acceptance of B05-P3, completion
and independent acceptance of B05-P4, frozen exact mapping code identity,
independently accepted safe configuration binding, a new route selection, a
fresh exact risk approval, and a fresh Goal.

The future protected counters are frozen:

```text
fresh artifact identity verifications = 1
direct artifact opens = 1
direct artifact reads = 1
direct artifact reopens = 0
direct SHA-256 computations = 1

B05 GET attempts = 1
completed B05 GET maximum = 1
automatic retries = 0
second GETs = 0

B01 staging-builder calls = 1
B03 projection-builder calls = 1
internal HTTP calls from B05 service = 0
F10 service calls = 0

persistence writes = 0
review-decision writes = 0
Evidence Layer writes = 0
production objects = 0
public/export/delivery actions = 0
```

The future smoke route is exactly
`GET /api/v1/internal/alpha/review-console/local-exchange-projections/{sample_handle}`
with `helldivers2-psn-demo`. It must not replay the filename-based B03 route as
a substitute and must not issue both routes. No B05-P5 smoke is selected or
authorized now.

## 15. Future runner ordering and recovery boundary

A future separately approved P5 runner must:

1. verify repository, commit, mapping identity, accepted safe configuration
   binding, and exact runner identity before protected access;
2. import or build the application only under explicit P5 authority;
3. create and initialize the approved event loop when required;
4. install type-preserving network and request instrumentation only after
   application and event-loop initialization;
5. perform one fresh artifact identity read and SHA-256 verification;
6. perform exactly one B05 GET;
7. emit one bounded safe result;
8. restore process-local state and close the event loop;
9. never retry after protected access starts.

B05-P3 performs none of these actions. If protected action may have started and
the state becomes ambiguous, the prior approval and Goal remain consumed, no
retry is allowed, and fresh narrow recovery governance is required.

## 16. Fail-closed matrix

| Condition | Required stop boundary | Forbidden follow-up |
| --- | --- | --- |
| Artifact missing | Stop before B05 GET | No discovery, alternate root, replacement, or retry |
| Artifact SHA mismatch | Stop before B05 GET and before JSON parse | No mapping substitution or retry |
| Configuration binding absent or mismatched | Stop before protected access when safely detectable, otherwise before B05 GET | No client override or alternate configuration |
| Registry mapping mismatch | Stop before protected access | No alternate handle or basename |
| Disabled gate | Stop before B01/B03 | No automatic gate change |
| Unknown or malformed handle | Return the exact B03 52-field fail-closed sentinel | No alternate mapping lookup |
| B01/B03 call-count deviation | Stop | No second request or repair call |
| Response schema, order, or count deviation | Reject the response | No partial acceptance, F10 conversion, or second request |
| Ambiguous protected-action state | Classify as post-protected ambiguity when action may have started | No retry; fresh recovery governance required |
| Privacy or path exposure | Set `privacy_issue_stop = yes` | No commit claiming runtime readiness |

Every failure remains bounded, non-mutating, and non-substituting. No failure
authorizes discovery, fallback, a second request, a trust upgrade, persistence,
or public/export/delivery action.

## 17. Privacy and no-path-exposure boundary

The future mapping exposes only the safe handle at the client boundary. The
basename is server-owned and may not be rendered, logged, stored in browser
state, or accepted from a client. Configuration values, external roots,
absolute paths, package contents, source payloads, response bodies, credentials,
and environment values remain outside this contract.

The B04 artifact basename and SHA-256 appear only as explicitly approved
governance identities. No external absolute path is recorded. Any privacy or
path exposure is an immediate stop and cannot be redacted after execution to
retroactively make a smoke acceptable.

## 18. Independent acceptance and phase sequencing

The only valid sequence is:

1. B05-P3 docs-only contract completion and ready-only Git finalization;
2. independent ChatGPT acceptance of B05-P3;
3. separate selection, exact approval, and fresh Goal for B05-P4;
4. B05-P4 no-runtime implementation and independent acceptance;
5. separate safe configuration-identity governance when still unavailable;
6. separate risk route selection, exact approval, and fresh Goal for B05-P5;
7. at most one protected P5 smoke under its exact counters.

No phase starts automatically. B05-P3 acceptance would make P4 eligible for
later selection only; it would not authorize implementation or runtime access.

## 19. Exact current and future authorization state

```text
MVP_B05_P3_status = candidate_completed_pending_independent_ChatGPT_acceptance
MVP_B05_P3_approval_reusable = no
MVP_B05_P3_Goal_reusable = no

real mapping currently implemented = no
real mapping currently present = no
real mapping currently authorized for runtime = no
real mapping currently enabled = no

future mapping candidate defined = yes
future mapping candidate implemented = no
future mapping candidate activated = no

MVP_B05_P4_candidate_defined = yes
MVP_B05_P4_selected = no
MVP_B05_P4_authorized = no
MVP_B05_P4_executed = no

MVP_B05_P5_candidate_defined = yes
MVP_B05_P5_selected = no
MVP_B05_P5_authorized = no
MVP_B05_P5_executed = no
```

This contract does not claim independent acceptance and grants no artifact,
package, endpoint, application-import, mapping-activation, persistence,
production, public, export, or delivery authority.

## 20. No-runtime/no-side-effect ledger and next boundary

```text
application imports = 0
tests = not_run
py_compile = not_run
frontend build = not_run
browser activity = 0
artifact/package/runtime accesses = 0/0/0
endpoint calls = 0
provider/collector/network/LLM calls = 0/0/0/0
database/SQLite/persistence accesses = 0/0/0
product code/test/config/route/API/frontend changes = no
Project Source changed = no
tag = none
release = none
```

The exact B05-P3 changed-file allowlist is this document only:

```text
docs/architecture/sentigraph_mvp_b05_p3_one_server_owned_real_sample_mapping_authorization_contract_v1_0.md
```

Next boundary is independent ChatGPT acceptance. Do not implement B05-P4,
enable a real mapping, access or rehash the artifact, call the B05 endpoint, or
perform persistence, production, public, export, or delivery work.
