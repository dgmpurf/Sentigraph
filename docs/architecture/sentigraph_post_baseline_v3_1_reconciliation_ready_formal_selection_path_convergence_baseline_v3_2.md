# Sentigraph Baseline v3.2 Formal Selection Path Convergence and Protected Execution Establishment

## 1. Purpose and Authority Boundary

This document establishes repository governance for Baseline v3.2 after the
independently accepted Baseline v3.1 Console Reconciliation result. It is a
docs-only establishment record.

```text
document classification =
docs-only governance baseline establishment

baseline version =
3.2

runtime authority created =
no

candidate-read or candidate-execution authority created =
no

real B05 GET or screenshot authority created =
no

status after ready repository completion =
effective_pending_independent_ChatGPT_acceptance
```

The exact approval authorizes only this architecture document and its
ready-only stage, commit, and push. It does not select or authorize any
Baseline v3.2 execution track.

## 2. Baseline v3.1 Historical Closure

```text
Baseline v3.1 status =
historically_closed_and_superseded_before_protected_execution

Baseline v3.1 objective completed =
no

Baseline v3.1 historical results reclassified =
no

track counters reconciliation / protected / recovery =
1 / 0 / 0

remaining before supersession =
0 / 1 / 1
```

The unused reservations are retired as follows:

```text
unused v3.1 Protected reservation =
retired / nonreusable / not consumed as an execution

unused v3.1 Recovery reservation =
retired / nonreusable / not consumed as an execution
```

Neither unused reservation was selected, authorized, Goal-authorized,
executed, or consumed as an execution.

## 3. Preserved Baseline v3.1 Console Reconciliation Result

The accepted Baseline v3.1 result remains distinct and unchanged:

```text
status =
ready_expected_b05_abort_console_signal_reconciled_zero_b05

independent acceptance =
ready_and_independently_accepted

consumed / reusable =
yes / no

console before / after / delta / total =
0 / 1 / 1 / 1

target request-failed events =
1

expected / unexpected console signals =
1 / 0

event order =
selection_started__target_aborted__target_request_failed__state_confirmed

selection completed / selected state confirmed =
true / true

B05 route interception / outbound / response =
1 / 0 / 0

backend B05 GET / HTTP 200 =
0 / 0

screenshot =
0

page errors / unhandled rejections =
0 / 0

cleanup =
pass
```

This result established the bounded diagnostic reconciliation only. It did
not establish a real B05 response, a screenshot, formal selection-path
convergence, Protected Execution completion, persistence, production
readiness, or public readiness.

## 4. Independently Verified Candidate Identities

The following identities are documentary inputs independently verified before
this docs-only Goal. This Goal does not read or inspect either candidate.

```text
Controller filename =
sentigraph_baseline_v3_1_console_reconciliation_business_controller_v4.py

Controller bytes / SHA-256 =
52676 /
64fbc3a49bee66c9140c0b67ee536f475e83331adc04e5ccf03419ecdb239ec4

Controller encoding / BOM / AST =
strict UTF-8 / absent / pass

Adapter filename =
sentigraph_baseline_v3_1_console_reconciliation_adapter_v5.py

Adapter bytes / SHA-256 =
35572 /
302d5075486e2d915e1bb6bae34aa8a02445cca2049b622c988afcd0992ddaa5

Adapter encoding / BOM / AST =
strict UTF-8 / absent / pass

Adapter-to-Controller identity binding =
pass

repository-external =
yes

Protected Execution authority =
none
```

The bounded previous diff remains classified within the approved Baseline
v3.1 reconciliation scope only:

```text
Controller additions / removals =
149 / 30

Adapter additions / removals =
37 / 25

classification =
approved v3.1 reconciliation scope only
```

## 5. Privacy-safe Static Review and Formal-path Divergence

### Privacy-safe static-review record

```text
console message text reads =
0

console message args reads =
0

console message location reads =
0

raw console text retained or output =
no

request URL handling =
parsed in memory only for scheme, loopback-host and exact target-path
classification

raw request URL retained or output =
no

formal total-console-error requirement =
zero and unchanged

Adapter formal ready matrix =
total console_error_count equals zero
```

### Distinct frozen selection paths

```text
proven diagnostic selection path =
Ant Design combobox /
keyboard ArrowDown open /
visible rc-trigger portal /
rc-virtual-list option located by stable title /
option click /
tracked heading confirmation

frozen formal selection path =
generic aria-label locator click /
exact visible-text option click
```

The resulting eligibility finding is:

```text
formal path exercised by the zero-B05 reconciliation diagnostic =
no

divergence classification =
unvalidated formal-path convergence gap

proof that the formal path will fail =
no

Protected Execution eligibility after static review =
not yet established
```

This static finding does not reclassify the accepted Baseline v3.1
Reconciliation result. It establishes a new eligibility boundary: Protected
Execution is one-shot and cannot repair implementation, so its sole real B05
attempt must not be spent on a selection implementation that differs from the
proven zero-B05 mechanism and has not itself been rehearsed.

## 6. Baseline v3.2 Definition

```text
baseline name =
sentigraph_formal_selection_path_convergence_then_protected_execution

baseline version =
3.2

status after ready repository completion =
effective_pending_independent_ChatGPT_acceptance

execution model =
formal_selection_convergence_then_protected_plus_recovery

starting convergence / protected / recovery counters =
0 / 0 / 0

budget convergence / protected / recovery =
1 / 1 / 1

remaining convergence / protected / recovery =
1 / 1 / 1
```

Baseline establishment consumes none of the Convergence, Protected Execution,
or Recovery reservations.

## 7. Baseline v3.2 Reservation and Authorization Matrix

| Track | Planning capacity only | Selected | Authorized | Goal-authorized | Executed | Reusable execution authority |
|---|---|---|---|---|---|---|
| Formal Selection Path Convergence | yes | no | no | no | no | none |
| Protected Execution | yes | no | no | no | no | none |
| Recovery | yes | no | no | no | no | none |

No track starts automatically, inherits authority, or becomes executable
through creation of this document.

## 8. Formal Selection Path Convergence Track

Exactly one future Goal is reserved:

```text
candidate pairs maximum =
1

zero-B05 formal-path rehearsals maximum =
1
```

Its repository-external output filenames are:

```text
sentigraph_baseline_v3_2_formal_selection_convergence_business_controller_v5.py

sentigraph_baseline_v3_2_formal_selection_convergence_adapter_v6.py
```

The objective is to make the future formal Protected path use the exact proven
Ant Design selection mechanism, or one shared bounded selection helper used
identically by the diagnostic and formal paths, and then rehearse that exact
future-formal selection function once with zero B05.

The governing identity invariant is:

```text
selection function rehearsed with zero B05 =
exact selection function used by future formal Protected Execution
```

The future Convergence ready matrix is:

```text
candidate pairs =
1

zero-B05 formal-path rehearsals =
1

combobox ready =
true

popup ready =
true

virtualized option ready =
true

selection call completed =
true

selected state confirmed =
true

B05 interception / outbound / response =
1 / 0 / 0

backend B05 GET / HTTP 200 =
0 / 0

expected / unexpected diagnostic abort signals =
1 / 0

future formal total-console-error criterion =
0

page errors / unhandled rejections =
0 / 0

screenshot =
0

cleanup =
pass

repository / Project Source / Git writes =
0 / 0 / 0
```

Permitted future changes are limited to extracting or defining one shared
bounded Ant Design selection helper; using that exact helper from formal and
diagnostic flows; retaining the already accepted diagnostic-only
abort-correlation evidence; and performing the result-envelope, schema,
parser, identity-binding, and cleanup bookkeeping necessary for convergence
validation.

The future Convergence Goal must not change tracked product code or the
Business API; weaken CIB, gates, privacy, GET counts, acceptance criteria, or
the formal zero-console-error requirement; perform a real B05 GET or
screenshot; substitute a dependency or browser; or create more than one
candidate pair or rehearsal.

Its hard-zero boundary is:

```text
real B05 GET / B05 HTTP response / screenshot =
0 / 0 / 0

tracked repository / Project Source / Git writes =
0 / 0 / 0

Provider / Collector =
0 / 0

database / persistence mutation =
0 / 0

production / public / export / delivery =
0 / 0 / 0 / 0
```

## 9. Protected Execution Track

Protected Execution becomes eligible only after all of the following:

```text
ready Formal Selection Path Convergence
frozen exact Controller V5 / Adapter V6 identities
independent ChatGPT acceptance
fresh exact Protected Execution approval
fresh Goal
```

Its future one-shot invariant is:

```text
retained runtime receipt reads =
1

stable CIB receipt reads =
1

configuration reads / CIB computations =
3 / 1

gate prestate / temporary overrides / restores =
5 / 5 / 5

backend / frontend starts =
1 / 1

loopback navigation / refresh / selection =
1 / 0 / 1

B05 GET attempts / completed / retries / second GET =
1 / 1 / 0 / 0

B05 HTTP 200 =
1

screenshot attempts / completed / second screenshot =
1 / 1 / 0

formal console errors / page errors / unhandled rejections =
0 / 0 / 0

visible safety assertions =
all pass

privacy boundary =
pass

complete browser, server, port, gate and process cleanup =
required
```

Protected Execution must use the exact frozen Baseline v3.2 candidate pair and
the exact rehearsed selection function. It may not repair implementation. A
required implementation change requires a new baseline.

## 10. Recovery Track and Track Separation

```text
Recovery budget =
1

Recovery selected at establishment =
no

Recovery authorized at establishment =
no

Recovery purpose =
post-protected recovery only
```

Ordinary pre-protected engineering work must not consume Recovery.

```text
Formal Selection Path Convergence =
may repair and rehearse with zero B05 /
cannot perform real B05 GET or screenshot

Protected Execution =
may perform exact one-shot business action /
cannot repair implementation

Recovery =
post-protected only
```

No track starts automatically and no track inherits authority.

## 11. Trial and Project Source Lifecycle

```text
v3.2 model =
effective for Baseline v3.2 only /
trial-only /
noncanonical

Project Source update during this Goal =
no

Source reconsideration point =
after stable independently accepted Baseline v3.2 terminal checkpoint and
review of external governance-research results in the next Chat
```

This document does not claim Canonical Source adoption.

## 12. Docs-only No-side-effect Record

```text
runtime / protected / candidate actions =
0 / 0 / 0

CIB / configuration / gates =
0 / 0 / 0

servers / browser / B05 GET / screenshot =
0 / 0 / 0 / 0

Provider / Collector =
0 / 0

database / persistence =
0 / 0

production / public / export / delivery =
0 / 0 / 0 / 0

Project Source reads / changes =
0 / 0

product code / tests / configuration changes =
0 / 0 / 0
```

The sole repository change authorized by this Goal is this architecture
document. Baseline v3.2 remains pending independent ChatGPT acceptance, the
current selected track is none, and next-stage execution authority is none.
