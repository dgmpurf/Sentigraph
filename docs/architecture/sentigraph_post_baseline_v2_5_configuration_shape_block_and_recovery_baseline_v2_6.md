# Sentigraph Post-Baseline v2.5 Configuration-shape Block and Recovery Baseline v2.6

## 1. Baseline identity and document state

```text
Baseline name =
Sentigraph Baseline v2.6 Configuration-shape Recovery Governance

Baseline version = 2.6

document state at commit =
baseline_v2_6_establishment_candidate_pending_independent_acceptance

intended effective state after independent ChatGPT acceptance =
effective_and_independently_accepted

repository =
dgmpurf/Sentigraph

starting branch / HEAD =
main / ea50526edb53632c80a31654034aaa5d3b1d286c
```

This document establishes a governance candidate. It does not claim independent
acceptance, select an engineering route, or authorize protected or runtime work.

### Approval and contract identities

```text
exact approval received = yes

Compact approval phrase SHA-256 =
0b40e06beabd8e7d306a44d29579e3aaff834728aa10ed3777a8a497ac0c8401

Baseline v2.6 Governance Contract V1 SHA-256 =
c65107badac7494153673534d881fe8fe3105e5f84d73e5015821fa342dccfde
```

The approval authorizes exactly one fresh Goal, this one new docs-only
architecture file, bounded docs-only validation, and ready-only commit and push.
It does not authorize Project Source access, receipt access, environment or HKCU
access, CIB work, Runner execution, product import, GET, application-importing
tests, or runtime activity.

## 2. Repository and active Source governance anchors

### Repository anchor

```text
repository = dgmpurf/Sentigraph
starting branch = main
starting HEAD = ea50526edb53632c80a31654034aaa5d3b1d286c
starting message = Qualify B05 GET smoke auditor and runner v3 rc1 compatibility
```

### Bound active Source identities

```text
active Source count = 10
active Canonical indices = 00 through 09
Canonical 10 = absent

Canonical 00 =
bytes 116979
SHA-256 2556641b84fb438169df985146203720336af76e9deb7ef47030f7b75cf207eb

Canonical 08 =
bytes 103846
SHA-256 0c38c24ed5f1ce02c33c814d5638d90151ebaa93632699266fbf46a032b83fdd

Canonical 09 =
bytes 94083
SHA-256 8b99a2a4c8e083ce1e499669dd8d2f4e5363a59a1fcc018c245c7bb8803dac4a
```

These identities were supplied by the independently accepted Source
synchronization and are recorded as governance evidence only. Codex did not
locate, read, or modify active Project Source. Establishing this Baseline
candidate does not trigger another Source update or create Source authority.

## 3. Baseline v2.5 historical closure

Baseline v2.5 remains closed, exhausted, and unreclassified:

```text
status = historically closed and exhausted

final engineering / fixed / conditional / risk =
4 / 1 / 1 / 2

remaining fixed / conditional / risk =
0 / 0 / 0
```

Unused capacity does not transfer to Baseline v2.6. All Baseline v2.5
approvals and Goals remain consumed, nonreusable, and unavailable as authority
for any future step.

### Fixed Prompt 1 historical record

```text
Decision = needs_fix
classification =
needs_fix_runner_v3_current_product_contract_compatibility
consumed / reusable = yes / no
historical result reclassified = no
```

### Conditional Prompt 1 historical record

```text
Decision = ready
status = completed_and_independently_accepted
subject =
Auditor V3 RC1 and Runner V3 RC1 current-product contract compatibility qualification
completion commit =
ea50526edb53632c80a31654034aaa5d3b1d286c
consumed / reusable = yes / no
historical result reclassified = no
```

### Risk Prompt 1 historical record

```text
Decision = blocked
classification = blocked_execution_integrity
status =
completed_and_independently_accepted_as_terminal_safe_block
Runner executions / retries = 1 / 0
safe result / partial result = absent / absent
consumed / reusable = yes / no
historical result reclassified = no
```

### Risk Prompt 2 historical record

```text
Decision = blocked
classification = blocked_configuration_shape
status =
completed_and_independently_accepted
receipt identity / UTF-8 / JSON / public contract =
pass / pass / pass / pass
environment reads / enumeration = 3 / 0
configuration shape = fail
canonical objects / serializations = 0 / 0
CIB comparisons = 0
application imports / event loops / Runner executions / GET attempts =
0 / 0 / 0 / 0
files created = 0
consumed / reusable = yes / no
historical result reclassified = no
```

Independent acceptance of a blocked or needs-fix history accepts its evidence
and classification. It does not convert that history into successful execution
or authorize a retry, repair, or later stage.

## 4. Directly established configuration-shape blocker

The independently accepted Baseline v2.5 Risk Prompt 2 evidence establishes
exactly the following:

- the accepted receipt identity and public contract passed;
- exactly three approved environment values were read;
- at least one value failed public configuration-shape validation;
- the specific variable remains unknown;
- the specific failure subtype remains unknown;
- canonical serialization was not reached;
- CIB comparison was not reached;
- current CIB equality is unknown;
- application import, event-loop creation, Runner execution, and GET were zero;
- no result file was expected or created on the configuration-shape block;
- no CIB recapture occurred.

The evidence does not establish:

- which variable failed;
- that the accepted receipt is stale;
- that current CIB matches or mismatches;
- that Runner RC1, the application, the route, the 52-field response, or
  Provider Result is defective;
- that GET occurred.

No product-capability or product-defect conclusion may be inferred from the
configuration-shape block.

## 5. Baseline v2.6 accounting

```text
initial engineering / fixed / conditional / risk =
1 / 0 / 0 / 0

budget fixed / conditional / risk =
2 / 2 / 3

remaining fixed / conditional / risk =
2 / 2 / 3
```

The docs-only Baseline establishment is engineering count 1. Fixed,
Conditional, and Risk budget is planning capacity only. No reservation becomes
selected, authorized, Goal-authorized, or executed merely because this
Baseline candidate exists or is later independently accepted.

Baseline v2.5 capacity does not transfer. Its approvals, Goals, runtime state,
and historical artifacts remain nonreusable.

## 6. Ordered configuration recovery route

The following seven reservations are ordered. Each is a separate possible
future governance route, and none is currently selected or authorized.

### 1. Fixed Prompt 1 — Per-variable Configuration-shape Diagnostic Static Qualification

Purpose:

- design and statically qualify a new repository-external diagnostic runner and
  a committed static auditor;
- classify each exact approved variable without exposing its value;
- prove that the diagnostic reads exactly the three approved names and no
  others;
- perform no protected execution.

The three public contract identifiers are:

```text
SENTIGRAPH_LOCAL_EXCHANGE_RESULTS_DIR
SENTIGRAPH_PRIVATE_COLLECTOR_EXPORT_ROOT
SENTIGRAPH_LOCAL_EXCHANGE_ADAPTER_ID
```

Future safe per-variable classifications are bounded to labels such as:

```text
shape_valid
missing
empty
leading_or_trailing_whitespace
nonprintable_or_nul
over_public_bound
adapter_format_invalid
diagnostic_integrity_block
```

The diagnostic must not disclose lengths, values, paths, or adapter identity.

### 2. Risk Prompt 1 — One Protected Per-variable Configuration-shape Diagnostic

Purpose:

- execute the independently accepted Fixed Prompt 1 diagnostic exactly once;
- read the three approved environment names once each;
- return only the three ordered bounded shape labels;
- perform no HKCU read, repair, application import, CIB operation, Runner
  execution, or GET;
- permit zero retry.

Eligibility requires independent acceptance of Fixed Prompt 1 followed by a
separate explicit route selection, fresh exact approval, and fresh Goal.

### 3. Conditional Prompt 1 — Authoritative Configuration-source Diagnosis

Purpose:

- proceed only after Risk Prompt 1 identifies the failed shape class;
- determine the authoritative persistent source for the three values under a
  separate bounded contract;
- distinguish process inheritance from persistent-source state;
- repair nothing;
- expose no values or paths;
- stop when the source is absent, conflicting, ambiguous, or unauthorized.

HKCU must not be assumed authoritative without separately governed evidence.

### 4. Risk Prompt 2 — One Bounded Configuration Repair

Purpose:

- proceed only after Conditional Prompt 1 establishes one authoritative source
  and exact repair semantics;
- repair only the exact affected configuration scope;
- use no alternate names, source discovery, or fallback;
- verify bounded readback without disclosure;
- perform no automatic CIB recapture;
- perform no Runner execution, application import, or GET;
- permit zero retry after mutation begins.

A current-process-only mutation is insufficient when the approved repair
contract requires durable process inheritance.

### 5. Conditional Prompt 2 — Post-restart Inheritance and CIB Comparison

Purpose:

- proceed only after Risk Prompt 2 is independently accepted;
- prove that a fresh process inherits all three shape-valid values;
- validate the accepted receipt contract;
- perform one CIB comparison;
- disclose no values, salt, or combined binding;
- perform no application import or GET.

Outcomes must distinguish at least:

```text
ready_post_restart_inheritance_and_cib_match
blocked_post_restart_configuration_shape
blocked_cib_mismatch
blocked_conditional_integrity
```

A mismatch does not authorize recapture. It stops Risk Prompt 3 eligibility
unless a later separately established Baseline or route addresses it.

### 6. Fixed Prompt 2 — Stage-specific Runner/Auditor RC2 Static Qualification

Purpose:

- create and statically qualify new RC2 identities;
- preserve the accepted receipt and 52-field projection contracts;
- make protected failure stages privacy-safe but diagnostically distinct;
- make result-transport failure reachable through a dedicated error class;
- ensure outermost bounded terminal reporting where safely possible;
- execute no Runner, application, or GET.

RC2 stage labels should safely distinguish categories such as:

```text
receipt_contract
configuration_shape
cib_mismatch
application_import
event_loop_or_guard
get_transport
http_status
projection_contract
response_bound
safe_result_write
restoration
result_transport
```

No label may disclose a value, path, response body, traceback, or protected
identifier.

### 7. Risk Prompt 3 — One Governed B05 GET Smoke RC2

Purpose and eligibility:

- become eligible only after Conditional Prompt 2 establishes post-restart
  shape validity and CIB equality;
- become eligible only after Fixed Prompt 2 RC2 is independently accepted;
- execute one Runner process;
- attempt one GET;
- permit zero automatic retry;
- create one bounded safe result on success;
- return a terminal privacy-safe stage classification on block;
- perform no database, persistence, production, export, or delivery action.

Eligibility is not selection or authorization.

## 7. Progression gates

The route is governed by these hard rules:

```text
Fixed Prompt 1 acceptance
does not authorize Risk Prompt 1

Risk Prompt 1 acceptance
does not authorize Conditional Prompt 1

Conditional Prompt 1 acceptance
does not authorize Risk Prompt 2

Risk Prompt 2 acceptance
does not authorize Conditional Prompt 2

Conditional Prompt 2 acceptance
does not authorize Fixed Prompt 2

Fixed Prompt 2 acceptance
does not authorize Risk Prompt 3
```

Every execution requires:

- explicit route selection;
- fresh exact approval;
- fresh Goal;
- independent post-result ChatGPT acceptance.

No two protected stages may be merged for convenience. No safe block may be
automatically repaired or retried.

## 8. Current Baseline v2.6 zero-authority matrix

At establishment:

```text
current selected engineering route = none
current engineering authority = none
current protected-access authority = none
current receipt-read authority = none
current environment-read authority = none
current HKCU or persistent-source authority = none
current configuration-mutation authority = none
current CIB-compare authority = none
current CIB-recapture authority = none
current Runner authority = none
current application-import authority = none
current event-loop authority = none
current GET authority = none
current repository authority beyond this docs-only file = none
current Project Source authority = none
current database / persistence authority = none / none
current production / export / delivery authority = none / none / none
```

After independent Baseline acceptance, the next-default candidate is only:

```text
Baseline v2.6 Fixed Prompt 1 =
Per-variable Configuration-shape Diagnostic Static Qualification

selected / authorized / Goal-authorized / executed =
no / no / no / no
```

A planning default is not execution approval.

## 9. Security and privacy boundary

This governance candidate contains no receipt body, protected configuration
value, per-variable length, absolute local path, HKCU value, Provider Result or
package content, response payload, exception text or traceback, credential, or
private identity.

The three environment variable names in Section 6 are public contract
identifiers. Their inclusion does not disclose their values or authorize a
read.

No future stage may disclose values, protected identities, raw evidence, paths,
response bodies, or diagnostic tracebacks.

## 10. Source lifecycle

- this task does not modify active Project Source;
- active Source remains the accepted ten-file Canonical 00–09 set;
- no immediate Source update is required solely for this candidate Baseline
  document;
- Source synchronization should be reconsidered after Baseline v2.6 is
  independently accepted or after the first stable v2.6 runtime checkpoint;
- Codex must not generate Source replacement candidates under this
  establishment authority.

## 11. Establishment task boundary

This docs-only establishment performs:

- one new architecture document;
- docs-safe structural, encoding, content, allowlist, and Git diff validation;
- ready-only staging, commit, and push when every validation gate passes.

It performs no:

- protected access;
- receipt, environment, or HKCU read;
- CIB comparison or recapture;
- Runner or auditor creation;
- application import or event-loop creation;
- GET;
- product-code or test modification;
- Project Source read, modification, or candidate generation;
- database, persistence, production, export, or delivery action.

Product tests, backend tests, frontend tests or builds, browser tests, and tests
that import the application are outside this docs-only task and are not run.
